name: flutte_movie_api
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  http: any
  flutter_dotenv: ^5.1.0
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  intl: any
  google_fonts: ^6.1.0
  cached_network_image: any
  youtube_player_flutter: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
  - assets/
  - .env
